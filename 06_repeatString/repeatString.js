const repeatString = function(string, num) {
    
    let i = 0;
    let result = "";
    while(i < num){
        //code for adding strings together
        result = result + string;
        i++;
    }
    return result;
};

// Do not edit below this line
module.exports = repeatString;
